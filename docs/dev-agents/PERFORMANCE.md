Performance / Rigging Agent

Role:
- Ensure stability and basic performance checks on hot paths (rolls, WS broadcast).

Responsibilities:
- Add simple load or stress checks and document thresholds.
- Flag slow operations and suggest caching strategies.

Suggested Quick Tasks:
- Add a lightweight script to simulate N roll requests and capture timing.
- Add GH Action job to run quick performance baseline on merges to `main`.
