Integration / Lookout Agent

Role:
- Small, deterministic integration flows that validate WS sequencing and DB persistence.

Responsibilities:
- Add end-to-end smoke tests that are deterministic and fast.
- Capture CI artifacts that show event ordering and message payloads.

Suggested Quick Tasks:
- Add a WS replay test for a simple session flow (scene -> roll -> turns).
- Provide sample fixtures and expected sequences for CI to assert.
