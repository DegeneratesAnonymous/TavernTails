Docs / Purser Agent

Role:
- Maintains developer onboarding, quickstarts, and runnable test scripts.

Responsibilities:
- Keep `requirements-dev.txt` and simple `make` targets up to date.
- Document how to run contract tests and CI-equivalent commands locally.

Suggested Quick Tasks:
- Add `requirements-dev.txt` with `pytest` and developer deps.
- Add `Makefile` targets or a `scripts/` entrypoint for `make test-contracts`.
